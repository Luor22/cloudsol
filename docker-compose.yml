version: '3.8'
services:
  selfinformation:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: selfinformation-app
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=
    restart: unless-stopped